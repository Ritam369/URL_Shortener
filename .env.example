# MongoDB Connection String
MONGO_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/url-shortener?retryWrites=true&w=majority

# Port (Vercel will override this)
PORT=5000

# Add your MongoDB Atlas connection string above
# Get it from: https://cloud.mongodb.com/
# 1. Create a cluster
# 2. Go to "Connect" -> "Connect your application"
# 3. Copy the connection string and replace <password> with your actual password
